# Post-edit regression review

## Protected-commitment sweep (all verified intact)

| Commitment | Status | Location |
|---|---|---|
| Independently fixed conditioning | Intact | `reader-model-and-dynamics.tex:21–41`; reinforced `grammaticality-de-idealized.tex:184–188` |
| State/dynamics split | Intact | `reader-model:13–19`, `335–342` |
| Speaker evidence vs population state (vs analyst) | Intact | `reader-model:136–162` |
| Opportunity / counterfactual choice / outside option | Intact | `reader-model:344–368`; `root:497–501, 528` |
| Conditional, not generic, bifurcation | Intact | `reader-model:382–389, 402–419`; `root:998–999` |
| Stabilization/maintenance/control | Intact | `reader-model:190–196, 438–443`; `root:1094–1101` |
| Explicit failure conditions | Mostly intact; one gap flagged below |
| Both-licensed dative control | Intact | `root:681–695, 896–925` |
| Compatibility delimiters | Intact | `root:401–409` |
| Constructional vs structural coverage | Intact | `root:531, 889–894, 1003` |

No restructuring loss detected: the operator falsifier (`reader-model:72–77`), scope-existential condition (`reader-model:264–270`), and the three-event evidence stream (`reader-model:363–368`) all survived compression.

## MUST

**M1. Define "evidence concentration" at first predictive use.** Three headline predictions hang on it (rank order `root:630–637`; moribundity `root:668–679`; `reader-model:473–480`), but the term is never glossed in the math-free main text. The gloss already exists in other words ("how settled the evidence is", `reader-model:219`); weld them together. One clause at `root:624–626` (or first use in `reader-model:163–182`) suffices: e.g., "concentration: how tightly a population's licensing evidence settles on one outcome, as opposed to how much of it there is." Without this, a typical linguistics expert cannot evaluate the moribundity prediction, which is the paper's most novel empirical claim.

## SHOULD

**S1. L2 preemption transfer lacks a failure condition.** Every sibling prediction in §Predictions states one (repair type: `root:707–710`; re-licensing: `root:725–726`; intonational operators: appendix `1205–1207`). The L2 paragraph (`root:656–666`) gives only positive predictions. Add one sentence, e.g.: if early L2 rejection tracks L2 input frequency rather than L1 preemption mass in matched niches, the transfer claim fails.

**S2. Duplicate diagnosis of independent relative *whose*.** Core-components gives the full sparse-opportunity case (`root:463–484`), including the wrong-denominator point (`root:477–480`). The usage-based section then reargues nearly the same claim ("separate frequencies don't create occasions", `root:936–942`). Keep the second pass's unique payoff ("predicting when raw rarity becomes informative preemption") and compress the rest to a cross-reference. The third pass in §Generative (`root:820–826`) is fine: it serves positioning, not diagnosis.

**S3. "Preemption mass" is undefined jargon.** Used at `root:656` and `root:894` with no reader-facing definition. Either gloss it once (accumulated counterfactually attributable competitor wins) or replace with plain phrasing.

**S4. Three-problems-to-four-questions bridge.** §Impasse announces three problems (`root:299–302`: analysis exists, instructions agree, community licenses), but the model has four constitutive questions; saturation arrives unannounced at `reader-model:103–108`. The introduction previews all four (`root:285`), so the machinery isn't lost, but the local bridge is jagged. Half a sentence at `root:301` noting that the obligatoriness cases motivate a fourth, distinct question would close it.

**S5. Satiation rank-order prediction needs its falsifier surfaced.** The rank order is stated positively (`root:630–637`) and the manipulation contrast at `root:634–637`, but the failure condition (rank inversion: *whose*/*pobedit'* moving less than *sheeps* or confirmed preempted gaps) is left implicit. One sentence, parallel to S1.

## LEAVE ALONE

- **Conclusion.** Paragraph 2 does near-repeat §Projectible-use (`reader-model:173–182`), but paragraphs 3–4 add what only the conclusion can carry: the maintenance-vs-control evidential asymmetry (`root:1097–1099`) and the earned-economy closing. It summarizes the shape, not the sections. Leave.
- **Double-starred tense example** (`ex:tense` `root:246`, `ex:tense-sem` `root:393`). Repetition is pedagogically deliberate here: first as raw data, then reanalyzed. Standard practice.
- **LBE recap in §Generative** (`root:797–809`). Redundant with `root:488–514` in content but doing concession work ("OVMG retains the lesson"); trimming it would weaken the positioning.
- **Table placement.** `tab:reader-profiles` sits at the end of the state theory (`reader-model:311–333`) and is correctly invoked retrospectively by Diagnostics (`root:340–344`). Ordering is right.
- **Appendix Turkish case.** Correctly self-contained with its own falsifier (`root:1205–1207`); the operator/exponent distinction is applied consistently with `reader-model:127–134`.

## Followability verdict

An expert reader can follow the argument end to end: terms are coined where used, the state theory precedes diagnostics, and dynamics precede the predictions that depend on it. The two residual friction points are M1 and S3, both terminological rather than structural; fixing them costs under fifty words total.