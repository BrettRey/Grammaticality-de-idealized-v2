# Thirty-Second Adversarial Pass Synthesis

**Date:** 2026-06-09
**Trigger:** Follow the next-action item for unresolved prediction-test data pointers.
**Change type:** Source/data-pointer enrichment, no graph mutation.

## What Changed

This pass did not add a graph, alter a graph edge, or move any score. It tightened the two remaining
mixed prediction-test lanes:

- pronoun policy/audience design;
- rare transparent-relative opportunity thresholds.

The source registry now distinguishes local sources that supply construct pressure from sources that
could supply measurement. The relevant phenomenon cards now include concrete data pointers rather
than only source IDs.

## Pronoun/Audience Lane

`pronoun-personhood-proform-gender` already had strong support for designatum-driven pro-form
choice, personhood ascription, and reference-chain coherence. The added pointer is narrower: the
unresolved cell needs an audience/policy task, not another conceptual discussion of personhood.

The useful design would vary:

- audience or evaluator;
- institutional or policy framing;
- speaker and referent identity cues;
- judgment prompt, separating reference, naturalness, correctness, respect, and social sanction.

This keeps `AUD` honest. It can remain a scoped pronoun/reference module, but policy or respect
judgments should not be treated as direct evidence that reference tracking or community licensing
has changed.

## Transparent-Relative Lane

The rare transparent-relative lane now has two concrete local routes:

- `Transparent_free_relatives/two-kinds.tex` plus `coca-data/`: a COCA-backed verb-class asymmetry
  where category-flexible transparent relatives are productive with attributional verbs but checked
  `seem`/`appear` cases yield no genuine non-NP transparent relatives.
- `Independent_relative_whose_A_Bayesian_analysis_of_a_low-frequency_gap/` plus
  `Independent_relative_whose/items/all_items.csv`: a low-opportunity Bayesian framing and
  judgment-material pattern for independent relative `whose`.

The key threshold is not raw rarity. It is absence or attestation after a declared opportunity
denominator, false-positive filter, and competitor set.

## Result

Both unresolved prediction cells remain `mixed`, but now for operational rather than bookkeeping
reasons. The workbench has pointers to the next data that would move either cell:

- an audience/policy pronoun task;
- an opportunity-denominator corpus check or elicitation threshold for rare transparent relatives.

No score movement is justified.
