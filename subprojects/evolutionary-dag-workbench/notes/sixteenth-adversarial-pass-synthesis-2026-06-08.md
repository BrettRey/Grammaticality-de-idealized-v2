# Sixteenth Adversarial Pass Synthesis

**Date:** 2026-06-08
**Trigger:** Let the prediction-test layer run against the first unresolved evidence lanes.
**Change type:** Source-backed evidence update, no graph mutation.

## What Changed

This pass targeted the three unresolved prediction-test cells named after the fifteenth pass:

- pronoun policy/audience design;
- dialectal negative correction;
- rare transparent-relative opportunity thresholds.

No graph topology was edited and no score moved. The pass only tightened source pointers,
phenomenon-card evidence notes, prediction-test evidence statuses, and one scoped-module metadata
label.

The pass also exposed a bookkeeping issue: `audience-reference-tracking-candidate` already had the
right protocol-bound pronoun/pro-form evaluation but no scoped-module label. That evaluation now has
prediction tests and a `scope-only` decision, and the graph has a zero-score `scoped_module` label.

## Evidence Outcomes

`pronoun-policy-framing-prediction` remains `mixed`.

The local personhood/pro-forms paper strengthens the designatum, personhood, and reference-chain
case: English pro-form choice depends on how the discourse entity is conceptualized, and
chain-internal personal/non-personal mixing is constrained unless discourse reclassification is
available. That supports the card's reference/personhood half. It does not yet supply an
audience-design task showing how policy framing changes reported acceptability or attribution.

`negation-standard-correction-prediction` moved from `mixed` to `passed`.

CGEL's multiple-negation section gives the needed dialect/standard contrast: Standard English
multiple semantic negation is distinguished from negative concord in non-standard dialects, with
standard NPI alternatives for the negative-concord examples. This supports treating standard
correction as norm-centre/correction pressure while refusing to make it a direct polarity-licensing
diagnosis.

`transparent-rare-relative-prediction` moved from `inconclusive` to `mixed`.

The local transparent-free-relative and independent-relative-`whose` materials add corpus and
attestation pressure for opportunity-normalized reasoning. They support the claim that rarity and
absence need to be interpreted against constructional opportunity and information-structural
support. They do not yet provide a general threshold that would make rare transparent cases
categorically resolved.

## Current Held-Out Prediction Status

The six source-backed held-out prediction tests now stand at:

- four `passed`;
- two `mixed`;
- zero `inconclusive`.

This is progress, but not score movement. The remaining mixed tests are mixed for the right reason:
they identify missing empirical knobs rather than missing graph topology.

The AUD pronoun/pro-form evaluation adds three module-internal prediction tests:

- two `passed` tests for reference alignment and personhood reclassification;
- one `mixed` test for audience/policy framing.

## Next Pressure Point

The next useful run is not another graph. It is a small data-pointers pass:

- audience-design or policy-framing evidence for pronoun/pro-form judgments;
- corpus or elicitation thresholds for rare transparent-relative cases.

Only after one of those cells changes from `mixed` to `passed` or `failed` should the workbench
consider score calibration.
