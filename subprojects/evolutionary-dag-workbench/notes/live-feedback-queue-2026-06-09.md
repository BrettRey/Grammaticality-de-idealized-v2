# Live Feedback Queue

**Created:** 2026-06-09

This note preserves the still-live Claude/GPT-Pro feedback items without letting them displace the
current marginal path.

## Current Priority

Run the `AGR` COCA projection lane once the English-Corpora.org session is authenticated. This is
the first data-bearing chance to test whether a scoped module projects beyond card accommodation.

## Items Not To Lose

1. **Projection remains the binding gap.** `projective_power` is named and scaffolded, but it should
   not move until a registered confirmatory target survives data-bearing evaluation.
2. **Compression pressure is still missing.** After the first empirical lane, run an ablation/merge
   pass: identify whether any scoped modules can be killed, merged, or simplified without losing
   prediction-path coverage.
3. **LLM verdict variance is unmeasured.** Pick one card/evaluation and rerun the critic protocol
   several times, logging whether the survival verdict flips.
4. **Edge semantics must stay modest.** Relation profiles and computed path readings are
   pre-registration scaffolding, not full causal modelling. Mixed causal, measurement,
   constitutive, and evidential paths should not be advertised as intervention-licensed DAGs.

## Immediate Marginal Fix Applied

`ontology/edge-types.yaml` now defines `causal` as directional explanatory dependence rather than
as an interventionist DAG relation, matching the README disclaimer.

## Compression Screen Added

`notes/scoped-module-compression-audit-2026-06-09.md` records the first node-overlap compression
screen, and `scripts/audit_module_overlap.py` makes it reproducible. The screen does not justify
immediate merger, but it names two pressure pairs:

- `FDL` / `UOB`: best future merger test.
- `OPG` / `DYN`: best overfitting guard.
