# Twenty-Seventh Adversarial Pass Synthesis: Allowed Doing Cohort Contrast

**Date:** 2026-06-09
**Input:** Brett local contrast:

- for Brett: `We're not allowed to do that` / `*We're not allowed doing that`;
- for Brett's children: `We're not allowed to do that` / `We're not allowed doing that`.

## Added Card

Created `allowed-to-doing-cohort-contrast`.

This card is useful because it crosses two live modules: complement selection and
cohort-conditioned licensing. It is not just a selection card, and it is not just a diachronic or
community card.

## Evaluation

Added `selection-collocation-split-allowed-cohort-2026-06-09` against `SEL`.

## Result

`allowed-to-doing-cohort-contrast` partly survives `SEL`.

`SEL` correctly treats the contrast as argument-linking or complement selection rather than
payload choice. It also keeps reported acceptability from directly deciding community licensing.
The partial result matters because `SEL` lacks speaker/cohort identity, production probability,
and diachronic stabilization. Those omissions are exactly what the children's pattern pressures.

## Decision

No new graph yet.

The card should be used to test whether a later hybrid is warranted: selection machinery from
`SEL` plus context/diachronic machinery from `DYN`, without simply merging every useful construct
into one general graph.

## Next Pressure Points

- Add more predicate-plus-complement cohort contrasts before adding a cohort-selection graph.
- Test this card against `DYN` if the question becomes production stability or generational
  diffusion rather than complement selection.
- Keep adult correction separate from child-community licensing.
