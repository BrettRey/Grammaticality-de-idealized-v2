# Thirty-Sixth Adversarial Pass Synthesis

**Date:** 2026-06-09
**Trigger:** Convert the remaining pronoun/audience mixed cell into a runnable measurement design.
**Change type:** Task design, no graph mutation.

## What Changed

Added:

- `notes/pronoun-audience-policy-task-design-2026-06-09.md`

The design targets the `AUD` prediction test `audience-policy-framing-prediction` and the parallel
`DYN` held-out test `pronoun-policy-framing-prediction`.

## Main Rule

The useful evidence is not another general discussion of personhood or pronoun reference. The task
must hold the target string and reference relation steady while varying audience, evaluator,
institution, norm centre, and prompt framing.

The response channels must be separated:

- reference tracking;
- feature/personhood fit;
- reported acceptability or naturalness;
- grammaticality attribution;
- social sanction;
- norm orientation.

## Result

No graph changes and no score movement. The audience-policy prediction remains `mixed`, but it is
now operational: a future data pass can tell whether audience/policy framing changes social value,
acceptability, or attribution while reference success remains stable.

This keeps `AUD` scoped. It should not be allowed to treat all policy or respect effects as
licensing effects, and `DYN` should remain partial where the measured outcome depends on reference
tracking or pro-form feature alignment.
