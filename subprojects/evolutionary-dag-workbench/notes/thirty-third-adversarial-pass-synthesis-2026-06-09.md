# Thirty-Third Adversarial Pass Synthesis

**Date:** 2026-06-09
**Trigger:** Convert one unresolved data-pointer lane into a measurement design.
**Change type:** Measurement design, no graph mutation.

## What Changed

The rare transparent-relative lane now has an explicit measurement plan:

- `notes/transparent-relative-opportunity-measurement-design-2026-06-09.md`

The design uses two local source lanes:

- the COCA-backed transparent-free-relative verb-class materials;
- the independent-relative-`whose` Bayesian note and judgment-item table.

## Main Rule

The relevant quantity is not raw rarity. The design treats absence as probative only after a checked
opportunity denominator:

```text
opportunity_normalized_attestation =
  genuine_transparent_count / checked_opportunity_count
```

High checked opportunity plus zero genuine cases can undercut licensing for a subtype. Low
opportunity plus absence remains uncertainty. Positive but restricted attestation licenses only the
declared conditioning cell.

## Result

No graph changes and no score movement. `transparent-rare-relative-prediction` remains `mixed`,
but the next data pass is now specified well enough to run without inventing thresholds after the
fact.
