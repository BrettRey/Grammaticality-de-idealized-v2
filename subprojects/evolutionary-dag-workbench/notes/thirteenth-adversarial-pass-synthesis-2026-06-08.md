# Thirteenth Adversarial Pass Synthesis

**Date:** 2026-06-08
**Trigger:** `fused-head-nps-cgel`.

## Result

Do not add a new graph.

`fused-head-nps-cgel` stresses the difference between recoverability, constructional support,
category/function analysis, and genre-bound compression. The existing stack can already represent
the needed separation:

- `context-aware-operator-gap-candidate` survives as the best current module because it separates
  constructional analogy from semantic-pragmatic recoverability, keeps recoverability out of
  licensing, and includes genre/register and repair-pressure channels.
- `category-measurement-discipline-candidate` partly survives because it handles the analyst-label,
  constructional-function, and measurement-task hazards, but it lacks register/genre
  appropriateness and fused-head-specific support.
- `selection-collocation-split-candidate` partly survives because it has constructional function
  and genre fit, but its path routes function through argument-linking selection and it lacks
  semantic-pragmatic recoverability. Fused-head NPs are not just selected-complement or collocation
  cases.

## Why This Matters

This pass checks whether the workbench should add a fused-head construction module. It should not,
yet. The card is a good discriminator, but the current evidence shows complementary coverage rather
than a representational hole.

The most important constraint is negative: no evaluated graph may license fused-head cases by a
direct recoverability-to-licensing path or by treating the analyst category label as the target.

## Boundary

- `OPG` remains the best module for recoverability plus constructional support.
- `CAT` remains the category/function and measurement-discipline module.
- `SEL` remains a selection/collocation boundary module, not a fused-head module.

All numeric scores remain zero. No new scoped label is authorized.
