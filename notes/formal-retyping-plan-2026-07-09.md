# Formal Retyping Implementation Plan

I will apply the critique as a focused formal repair rather than a broad rewrite.

1. Retype licensing from binary `z` with a Beta “over z” to speaker-level binary inclusion plus population licensing rate `theta_t(kappa,c)`. The learner carries a Beta over `theta`; `C_t` is the posterior mean and `nu_t` is concentration. This fixes the ontology for Phi, framing, and gap/cell dissociation.

2. Recast bounded-memory Beta counts as a power-discounted dynamic filter for a drifting convention, not a literal stationary posterior over a fixed parameter. This legitimizes `delta_m` while preserving the existing moribundity result.

3. Replace the logistic/repair ODE with a mean-field normal form that can actually support bistability: evidence-balance term plus majority-enforcement repair term. Remove exogenous `C^*` from the repair dynamics and define the separatrix used by actuation.

4. Align production and omission likelihoods. State that omission weight is a first-order approximation in the high-confidence licensed regime; with uncertain production, `q_i` is attenuated and freezes near concentrated zero, which is why re-opening needs exogenous actuation.

5. Define stream-specific coverage weights: positive evidence via parsing posterior and negative/error evidence via likelihood sensitivity. This makes the LBE dedicated-node claim less stipulative.

6. Tighten smaller formal loose ends where local: define `Delta(d)` as expected common-ground divergence, note repair is not double-counted because the ODE term is the mean-field contribution generated by repair-weighted evidence streams, clamp/restrict `q_i`, call OBL/sat derived macros, and add a notation table row if needed.

I will not add the simulation appendix in this pass; it is a separate artifact and should not be mixed into the formal repair commit.
